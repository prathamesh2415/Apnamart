{
  "name": "apnamart",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "npm run prisma:generate -w marketplace-api",
    "build": "npm run prisma:generate -w marketplace-api && npm run build -w marketplace-web",
    "bundle:api": "node client/scripts/bundle-api.mjs",
    "db:migrate": "npm run prisma:deploy -w marketplace-api",
    "db:seed": "npm run db:seed -w marketplace-api"
  },
  "dependencies": {
    "@prisma/client": "6.19.3",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "esbuild": "0.25.9",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "prisma": "6.19.3",
    "zod": "^3.24.4"
  }
}
